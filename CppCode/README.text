A quick prototype that shows sqllite works in cpp

to run, \CppCode> .\query.exe

To compile, just use the make file (no need to do so to run as I am pushing the exe)
\CppCode> make
\CppCode> .\query.exe