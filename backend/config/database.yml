# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: sqlite3
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

# development/production point at the same committed library.db the old
# Sinatra app used (one level up from backend/), overridable via LIBRARY_DB
# exactly like app.rb's DB_PATH did — no data migration needed.
development:
  <<: *default
  database: <%= ENV.fetch("LIBRARY_DB") { Rails.root.join("..", "library.db") } %>

# Fully isolated from library.db — Rails owns this file, safe to erase/rebuild.
test:
  <<: *default
  database: storage/test.sqlite3

# SQLite3 write its data on the local filesystem, as such it requires
# persistent disks. If you are deploying to a managed service, you should
# make sure it provides disk persistence, as many don't.
#
# Similarly, if you deploy your application as a Docker container, you must
# ensure the database is located in a persisted volume.
production:
  <<: *default
  database: <%= ENV.fetch("LIBRARY_DB") { Rails.root.join("..", "library.db") } %>
